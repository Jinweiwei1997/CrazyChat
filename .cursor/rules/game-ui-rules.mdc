---
description: 现行界面与按钮规则，换窗口先读
alwaysApply: true
---

# 界面规则

界面、按钮、打开/返回/关闭状态以 [docs/game-ui-rules.md](docs/game-ui-rules.md) 为准。改 UI 前必须先读。`docs/桌面互动伙伴需求.md` 是过时草案。

- 新功能默认只做桌上头像；没点名不改麻袋。麻袋只显示头像和名字、可折叠/拖出，不加聊天或玩法。
- 设置界面已使用 `Resources/Prefab/UI/SettingsMenu.prefab`；其他界面后续逐个迁移，不要把设置改回运行时拼装或保留双轨 fallback。
- 聊天：头像上方单句气泡 → 单一聊天界面，不再区分简版和全历史；打开时显示打开前未读来信，无未读则显示对方最后一句，本次新收发继续追加。打开和发送后保持输入聚焦，Enter 发送、Esc 关闭。通道 1，无离线/云端。
- A/B：两张齐才启用；按下 B、全松开 A；启用后停用弹跳/翻转。只在桌上，走通道 2。
- 互动：悬停好友出四槽；现有番茄扣 1000 点击。Catalog + Fx + 通道 2。
- 对方看到同步内容的前提：对方开着本游戏，并把发送者头像放在桌上。
