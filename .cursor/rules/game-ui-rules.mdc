---
description: 现行界面与按钮规则，换窗口先读
alwaysApply: true
---

# 界面规则

界面、按钮、打开/返回/关闭状态以 [docs/game-ui-rules.md](docs/game-ui-rules.md) 为准。改 UI 前必须先读。`docs/桌面互动伙伴需求.md` 是过时草案。

- 麻袋保留现有头像、名字、最后消息摘要、未读角标和点击聊天，可折叠/拖出；其他新玩法默认只做桌上头像。
- 设置界面已使用 `Resources/Prefab/UI/SettingsMenu.prefab`；四个页签和对应页面预先做在 Prefab 里，运行时只加载接线，不要启动后再补页签/页面，也不要把设置改回运行时拼装或保留双轨 fallback。
- 设置与聊天统一使用 VS Code 式扁平视觉：Codicons PNG 随主题染色，16px 图标/约 28px 点击区，默认透明、悬停浅底，1px 分隔线与极小圆角。
- Windows 正文优先使用 Microsoft YaHei UI，项目内置 Noto Sans SC 只作后备；设置和聊天卡片保持 1:1 像素缩放，避免非整数 Transform 缩放导致文字发虚。
- 设置页签为游戏/显示/动态/系统。显示页编辑风格和 RGB 颜色；动态页设置 A/B 图，选图后在本页裁剪，不单独弹窗。
- 放大倍数只影响桌上头像及其附属名字、气泡、设置入口、互动环、按键图标；设置/聊天/麻袋等其他 UI 不缩放。设置面板切页保持固定尺寸；显示器下拉按已连接屏幕列出 1/2/3 并切换透明窗口。
- 设置入口齿轮图标固定 24px（点击区不变）；冒出的按键图标基础尺寸 48px，并继续跟随头像放大倍数。
- 托盘图标和全局右键长按 2 秒隐身是正式需求，不得删除。隐身后可再次长按或从托盘「取消隐藏」恢复；托盘右键保留「退出」。
- 聊天三态互斥：头像未读轮播气泡 → 简易聊天 → 完整历史。简易态从最早未读开始；无未读则从对方最后一句开始，连续显示到当前最后一条，不区分发送方。条数上限读 `OverlayConfig.maxCompactChatMessages`（默认 5），双方合计不超过该值，只保留最近消息，不强制保留被挤走的对方消息。本次新收发继续追加，简易窗口高度按实际消息内容收缩。简易态的历史按钮进入完整历史；完整态在发送按钮右侧显示返回按钮，可回到简易态。窗口使用 `Resources/Prefab/UI/ChatPanel.prefab`，打开和发送后保持输入聚焦，Enter 发送、Esc 关闭。通道 1，无离线/云端。
- 未读气泡固定为头像宽度的 2/3，使用半透明底色；多条未读按旧到新每 5 秒用向上翻页动画轮播，不按文字长度改变宽度。
- A/B：两张齐才启用；按下 B、全松开 A；启用后停用弹跳/翻转。只在桌上，走通道 2。
- 互动：悬停好友出四槽；番茄扣点击数读取 `OverlayConfig.tomatoTapCost`。Catalog + Fx + 通道 2。
- 对方看到同步内容的前提：对方开着本游戏，并把发送者头像放在桌上。
