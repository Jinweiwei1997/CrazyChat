---
description: Overlay 框架约定，避免平行造轮子
alwaysApply: true
---

# 框架

框架见 [docs/ai-code-guide.md](docs/ai-code-guide.md)；现行界面规则见 [docs/game-ui-rules.md](docs/game-ui-rules.md)

- 先改现有类，不要新 Bootstrap / Manager / 事件总线 / 第二套 UI 或 P2P
- 装配只在 `FriendOverlayView.Build`；View 只接线
- UI 迁移按界面逐个进行：设置使用 `Resources/Prefab/UI/SettingsMenu.prefab`，聊天使用 `Resources/Prefab/UI/ChatPanel.prefab`，两者的 `Create()` 都只加载和接线；其他界面保持现状
- UI 统一使用 uGUI `Text` 并挂已有层；已接入 Prefab 的界面不要再保留运行时 fallback 第二套结构
- 互动：Catalog 登记 + Fx + 通道 2；聊天通道 1
- 需求不清时：无房间；麻袋只沿用现有摘要、未读和聊天入口，其他玩法只做桌上头像；对方必须开着本游戏
