inputPopScale=1 # 按键气泡大小倍率，1 为原大小

avatarSnapStartDistance=30# 鼠标预挪目标距吸附位置多近时开始吸附（屏幕像素），0关闭

avatarSnapDistance=50 # 鼠标拉开解除吸附的距离（屏幕像素），实际至少比开始距离大1，0关闭

avatarSnapGap=0 # 最终吸附时两个头像边缘之间的间隔（屏幕像素）

selectionStarScale=1 # 左侧星星大小倍率，1 为原大小

selectionStarOffsetX=2 # 星星中心相对气泡左上角的横向偏移（像素），正右负左

selectionStarOffsetY=-2 # 星星中心相对气泡左上角的纵向偏移（像素），正上负下

# 每行一个节点，两列用空格或Tab分隔；每表2～64行，从0秒开始，时间严格递增且不超过60秒，节点间平滑过渡。
[inputPopRise]
# 时间(秒)    速度倍率(0～10，1为原速度)
0             0.9
0.5           1

[inputPopFade]
# 时间(秒)    透明度(0不透明，1全透明)
0             0
0.25          0.5
0.5           0.9
# 存活时间取透明度表的最后时间，到时消失；上移距离按速度累计，速度表结束后保持末速。
